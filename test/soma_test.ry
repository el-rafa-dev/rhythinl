;test package

using system.Rythin
from system get DATE

;from system.rythin get date
;from system get printnl  somente teste do lexer, printnl é função nativa da lang

;def soma:int = 1 + 2
; definindo ponteiro, pegando endereço e valor

def const *a:int = 1 ;definimos o valor original como const. então não pode ser alterada
;def *b:int = &a ;pegamos o endereço de a e atribuimos a b
;def c = $b ; pegamos o valor do endereço de b


;exemplo de loop:
;def a:int[] = [1, 2, 3]
;loop i:int in a.size -> [
;    printnl("A")
;]

;sempre tem que chamar a funcao principal, é nele onde ira comecar a execução principal do codigo!


def Main:func() -> [
    print("Hello World from RythinLang!\n")
    if (1 * 2) -> [
        
        fin 0
    ]
    fin 0
]
