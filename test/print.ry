;loop test

;struct sd {
;    def a : str = nil
;}

def main:func() -> [
    loop (a:int in 23) -> [
        if (a >= 21) -> [
            stop
        ] but (a <= 21) [
            continue
        ]
    ]
    return 0
]
